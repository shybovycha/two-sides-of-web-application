.row
    .col-xs-12.col-md-6.col-md-offset-6
        form.form-inline
            .form-group
                input.form-control(type="text" placeholder="From date")
            .form-group
                input.form-control(type="text" placeholder="To date")
            .form-group
                .btn.btn-primary Update

.row
    .col-xs-12
        .well
            a.btn.btn-primary(href="#/apps/{{ app.id }}/edit") Edit
            h1 {{ app.name }}
            em
                | You can track your application visitors with this URL:
                br
                | http://localhost:3000/track/{{ app.token }}

.row.m-t-md
    .col-xs-12
        h3 Visitors
        .well
            | Chart goes here

    .col-xs-12.col-md-6
        h3 Visitors by country

        table.table.table-bordered
            thead
                tr
                    th Country
                    th Visitors
            tbody
                tr(ng-repeat="stat in app.stats.byCountry")
                    td {{ stat.country }}
                    td {{ stat.amount }}
